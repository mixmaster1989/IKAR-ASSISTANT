"""
Вспомогательные функции для модуля core.
"""
import random
import re
import logging
from typing import Dict, List, Optional, Union, Any
from datetime import datetime, timedelta

logger = logging.getLogger("chatumba.utils")

def estimate_sentiment(text: str) -> float:
    """
    Простая оценка тональности текста.
    
    Args:
        text: Текст для анализа
        
    Returns:
        Оценка тональности от -1.0 (негативная) до 1.0 (позитивная)
    """
    # Простые списки позитивных и негативных слов
    positive_words = [
        'хорош', 'отлично', 'круто', 'супер', 'класс', 'прекрасно', 'здорово',
        'рад', 'счастлив', 'люблю', 'нравится', 'спасибо', 'благодар', 'молодец',
        'приятно', 'весело', 'смешно', 'улыбк', 'добр', 'помог', 'успех'
    ]
    
    negative_words = [
        'плох', 'ужас', 'отстой', 'дерьмо', 'говно', 'хуй', 'пизд', 'бля', 'нахуй',
        'грустно', 'печаль', 'ненавижу', 'злой', 'злюсь', 'раздраж', 'устал',
        'скучно', 'неприятно', 'обидно', 'тупой', 'глупый', 'идиот', 'дебил'
    ]
    
    # Приводим текст к нижнему регистру
    text = text.lower()
    
    # Считаем количество позитивных и негативных слов
    positive_count = sum(1 for word in positive_words if word in text)
    negative_count = sum(1 for word in negative_words if word in text)
    
    # Если нет ни позитивных, ни негативных слов, возвращаем 0
    if positive_count == 0 and negative_count == 0:
        return 0.0
    
    # Вычисляем оценку тональности
    total = positive_count + negative_count
    sentiment = (positive_count - negative_count) / total
    
    return sentiment

def should_remember(text: str, importance_threshold: float = 0.7) -> bool:
    """
    Определяет, стоит ли запомнить сообщение.
    
    Args:
        text: Текст сообщения
        importance_threshold: Порог важности (0.0 - 1.0)
        
    Returns:
        True, если сообщение стоит запомнить, иначе False
    """
    # Простая эвристика для определения важности сообщения
    
    # Длина сообщения (длинные сообщения обычно содержат больше информации)
    length_score = min(len(text) / 500, 1.0)  # Нормализуем до 1.0
    
    # Наличие вопросов (вопросы часто важны)
    question_score = 0.5 if '?' in text else 0.0
    
    # Наличие имен собственных (часто указывают на важную информацию)
    proper_nouns_score = 0.3 if re.search(r'[А-Я][а-я]+', text) else 0.0
    
    # Наличие чисел (часто указывают на факты)
    numbers_score = 0.2 if re.search(r'\d+', text) else 0.0
    
    # Вычисляем общую оценку важности
    importance_score = (length_score * 0.4 + 
                       question_score * 0.3 + 
                       proper_nouns_score * 0.2 + 
                       numbers_score * 0.1)
    
    return importance_score >= importance_threshold

def generate_response_delay() -> float:
    """
    Генерирует задержку перед ответом для имитации человеческого поведения.
    
    Returns:
        Задержка в секундах
    """
    # Базовая задержка от 1 до 3 секунд
    base_delay = random.uniform(1.0, 3.0)
    
    # С вероятностью 10% добавляем длинную задержку (5-15 секунд)
    if random.random() < 0.1:
        return base_delay + random.uniform(5.0, 15.0)
    
    return base_delay

def get_random_emoji() -> str:
    """
    Возвращает случайный эмодзи.
    
    Returns:
        Случайный эмодзи
    """
    emojis = [
        "😀", "😃", "😄", "😁", "😆", "😅", "🤣", "😂", "🙂", "🙃",
        "😉", "😊", "😇", "🥰", "😍", "🤩", "😘", "😗", "😚", "😙",
        "😋", "😛", "😜", "🤪", "😝", "🤑", "🤗", "🤭", "🤫", "🤔",
        "🤐", "🤨", "😐", "😑", "😶", "😏", "😒", "🙄", "😬", "🤥",
        "😔", "😕", "🙁", "☹️", "😣", "😖", "😫", "😩", "🥺", "😢",
        "😭", "😤", "😠", "😡", "🤬", "🤯", "😳", "🥵", "🥶", "😱",
        "😨", "😰", "😥", "😓", "🤗", "🤔", "🤭", "🤫", "🤥", "😶",
        "😐", "😑", "😬", "🙄", "😯", "😦", "😧", "😮", "😲", "🥱",
        "😴", "🤤", "😪", "😵", "🤐", "🥴", "🤢", "🤮", "🤧", "😷",
        "🤒", "🤕", "🤑", "🤠", "😈", "👿", "👹", "👺", "🤡", "💩"
    ]
    return random.choice(emojis)

def get_random_word() -> str:
    """
    Возвращает случайное слово.
    
    Returns:
        Случайное слово
    """
    words = [
        "хмм", "блин", "короче", "типа", "вообще", "прикол", "жесть", "капец",
        "офигеть", "пиздец", "охуеть", "бля", "нахуй", "хуйня", "говно", "дерьмо",
        "отстой", "круто", "класс", "супер", "норм", "окей", "ладно", "понятно",
        "ага", "угу", "не", "да", "может", "наверное", "возможно", "точно",
        "определенно", "абсолютно", "полностью", "совершенно", "кстати", "впрочем",
        "однако", "зато", "правда", "честно", "серьезно", "реально", "действительно"
    ]
    return random.choice(words)